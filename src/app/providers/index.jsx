// Application-level providers will be configured here
// Currently not in use - add providers as needed:
// - Theme provider
// - Authentication provider
// - Query provider
// - State provider
// - Error boundary

export const AppProviders = ({ children }) => {
  return <>{children}</>;
};

export default AppProviders;
