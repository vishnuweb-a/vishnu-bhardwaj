// The feature's public surface. Everything else in this directory is internal
// (CLAUDE.md section 5) - BytePet is self-contained and takes no props, so one
// export is the whole API.
export { BytePet } from './components/BytePet';
export { default } from './components/BytePet';
